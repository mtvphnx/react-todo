import {Element} from "../../components";
import './List.scss';

export const List = () => {
    return (
        <ul className="app-list list-group">
            <Element/>
            <Element/>
            <Element/>
        </ul>
    );
}
